import SwiftUI

struct FYPView: View {
    var body: some View {
        VStack {
            Text("FYP Feed (coming soon!)")
                .font(.title)
                .foregroundColor(.white)
            Spacer()
        }
        .background(Color.black.ignoresSafeArea())
    }
}
